# XploitRUM Backend Environment Variables
# Copy this file to .env and update with your production values

# Application
PROJECT_NAME=XploitRUM CTF Platform
VERSION=1.0.0
ENVIRONMENT=production
DEBUG=False

# Security - IMPORTANT: Generate secure random keys!
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=CHANGE_THIS_TO_A_VERY_LONG_RANDOM_STRING
JWT_SECRET_KEY=CHANGE_THIS_TO_ANOTHER_VERY_LONG_RANDOM_STRING
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# Database - Production (PostgreSQL)
DATABASE_URL=postgresql://xploitrum:your_db_password@localhost/xploitrum

# Database - Development (SQLite)
# DATABASE_URL=sqlite:///./xploitrum.db

# Email (SendGrid SMTP)
# Note: For SendGrid, username should be "apikey" and password is your SendGrid API key
SMTP_SERVER=smtp.sendgrid.net
SMTP_PORT=2525
SMTP_USERNAME=apikey
SMTP_PASSWORD=your_sendgrid_api_key_here
SMTP_TLS=true
SMTP_SSL=false
FROM_EMAIL=info@xploitrum.org
ADMIN_EMAIL=admin@xploitrum.org

# CORS - Update with your domains
CORS_ORIGINS=https://www.xploitrum.org,https://ctf.xploitrum.org,https://api.xploitrum.org
ALLOWED_HOSTS=www.xploitrum.org,ctf.xploitrum.org,api.xploitrum.org,xploitrum.org

# URLs
FRONTEND_URL=https://www.xploitrum.org
API_URL=https://api.xploitrum.org
CTF_URL=https://ctf.xploitrum.org
LAB_URL=https://lab.xploitrum.org

# Docker
DOCKER_HOST=unix:///var/run/docker.sock
CHALLENGE_NETWORK=xploitrum_challenges
CHALLENGE_SUBNET=172.20.0.0/16

# OpenVPN
OPENVPN_SERVER_NAME=xploitrum
OPENVPN_PROTOCOL=udp
OPENVPN_PORT=1194
OPENVPN_NETWORK=10.8.0.0
OPENVPN_SUBNET=255.255.255.0

# CTF Configuration
MAX_CONCURRENT_CHALLENGES=50
CHALLENGE_TIMEOUT_HOURS=24
AUTO_CLEANUP_INTERVAL=3600

# Rate Limiting
RATE_LIMIT_PER_MINUTE=60
MAX_LOGIN_ATTEMPTS=5
LOCKOUT_DURATION_MINUTES=15

# File Upload
MAX_FILE_SIZE=10485760
UPLOAD_DIR=uploads

# Logging
LOG_LEVEL=INFO

